import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Aquí definiremos el estado inicial
};

const calidadSlice = createSlice({
  name: 'calidad',
  initialState,
  reducers: {
    // Aquí definiremos los reducers
  },
});

export const { } = calidadSlice.actions;
export default calidadSlice.reducer; 